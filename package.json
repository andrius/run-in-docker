{
  "scripts": {
    "build": "ncc build start/index.js -o start/start && ncc build start/cleanup.js -o start/cleanup && ncc build exec/index.js -o exec/exec && ncc cache clean"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@vercel/ncc": "^0.38.1"
  }
}
